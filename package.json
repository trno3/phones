{
    "name": "phones",
    "version": "0.0.1",
    "description": "Phones list demo",
    "main": "index.js",
    "scripts": {
        "start": "cd server && npm start",
        "start:dev": "cd client && npm start",
        "install:packages": "npm run install:client:packages && npm run install:server:packages",
        "install:client:packages": "cd client && npm i",
        "install:server:packages": "cd server && npm i",
        "build": "cd client && npm i && npm run build",
        "build:pipeline": "cd client && npm i && npm run build",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Toni Hervas",
    "license": "ISC"
}
